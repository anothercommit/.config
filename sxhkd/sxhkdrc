# Brillo
XF86MonBrightness{Up,Down}
    dbrillo {up,down}

alt + XF86MonBrightness{Up,Down}
    dbrillo {toggle,toggle}

# Audio
XF86Audio{Play,Next,Prev}
    playerctl {play-pause,next,previous}

XF86Audio{Mute,RaiseVolume,LowerVolume}
    dvol {toggle,up 5,down 5}

alt + XF86Audio{RaiseVolume,LowerVolume}
    dvol {up 20,down 20}

shift + XF86Audio{RaiseVolume,LowerVolume}
    dvol {up 1,down 1}

alt + b
    dbateria

# run
super + Return
    kitty

# super + k
    # kitty -e fzfnvim


alt + shift + e
    thunar

alt + shift + space
    kitty -e yazi

alt + shift + t
    /opt/google/chrome/google-chrome --profile-directory=Default --app-id=ficljmaaiddafabdgbkkgkgmogdgkibi

alt + shift + {b,o}
    {google-chrome-stable,obsidian}

alt + shift + p
    kitty -e nvim '/home/joaco/SecondBrain/1. Áreas 🛸/PKM/Palabras, inglés.md'

super + {_,shift} + v
    {copyq menu,copyq show}

control + space
    rofi -show drun

super + period
    rofimoji --skin-tone neutral -a type copy

alt + shift + l
    betterlockscreen -l dim

# Screenshots
Print
    maim | xclip -selection clipboard -t image/png

shift + Print
    maim -su | xclip -selection clipboard -t image/png

super + Print
    maim /home/joaco/Media/Imagenes/Capturas/$(date +%s).jpg

super + shift + Print
    maim -su /home/joaco/Media/Imagenes/Capturas/$(date +%s).jpg
